if (ENABLE_TESTING)
  add_subdirectory(llvm_ir)
  add_subdirectory(arith)
  add_subdirectory(mem_manager)
  if(POLYGEIST_SRC)
    message(STATUS "Polygeist found, building tests")
    add_subdirectory(polygeist)
  endif()
endif()
